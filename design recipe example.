def my_function(a, b):
   """ 
   >>> my_function(3,4)
   12
   >>> my_function('b', 6)
   'bbbbbb'
   """ 
   return a * b
       
def add(a, b):
    """
    >>> add(3,4)
    7
    >>> add('b', 'c')
    'bc'
    """
    return a + b

import doctest
doctest.testmod(verbose=True)
